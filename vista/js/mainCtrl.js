/*var app = angular.module("myApp", ["ngRoute"]);
app.config(function($routeProvider) {
    $routeProvider
    .when("/", {
        templateUrl : "registro.php"
        controller : "sesionCtrl"
    })
    .when("/profesores", {
        templateUrl : "/vista/profesores.php",
        controller : "profesoresCtrl"
    })
});/*